/**
 * Services barrel.
 *
 * Server-side service modules live in server/ — this file is kept
 * as a re-export shim for any code that imports from @/services.
 *
 * @see server/services/accounts.ts for the actual implementations.
 */
export * from '@/server/services/accounts';
